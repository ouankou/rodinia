Input video is generated locally (gitignored) via `scripts/generate_data.sh`
(or `scripts/generate_data.sh --only leukocyte`).
It produces `data/leukocyte/testfile.avi` at 640x480, 30 fps, 600 frames.
